{
  "compilerOptions": {
    /* === Configurações Básicas === */
    "target": "ES6",                          /* Converte para JS moderno mas compatível */
    "module": "ESNext",                       /* Usa módulos modernos (import/export) */
    "lib": ["DOM", "ES2020"],                 /* Libera o uso de document.querySelector e recursos novos */
    
    /* === O MODO ESTRITO (Seu pedido) === */
    "strict": true,                           /* Ativa TODAS as checagens de segurança */
    "noImplicitAny": true,                    /* Proíbe variáveis sem tipo definido (o famoso 'any') */
    "strictNullChecks": true,                 /* O erro mais comum: 'null is not an object' nunca mais! */
    
    /* === Organização === */
    "rootDir": "./src",                       /* Onde vamos colocar nosso código TS */
    "outDir": "./dist",                       /* Onde o JS compilado vai ser gerado */
    "removeComments": true,                   /* Limpa comentários do JS final (deixa mais leve) */
    "sourceMap": true                         /* Ajuda a debugar no navegador apontando pro TS */
  },
  "include": ["src/**/*"]
}